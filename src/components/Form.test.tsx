// when opening the page

// it should display a form with 2 text fields and a text message indicating if you are logged in or not

// it should simulate a backend call and add extra logic to a component

// it should trigger actions for submit and handle global state